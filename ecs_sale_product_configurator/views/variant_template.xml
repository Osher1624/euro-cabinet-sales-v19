<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!--
        NOTE: In Odoo 17+, the product configurator was rewritten as a pure OWL
        component. The QWeb template 'sale_product_configurator.configure_optional_products'
        no longer exists. The sale_attribute_ids pre-filling logic is now handled
        in the ecs_sale_product_configurator JS module via the OWL component patch.
        -->
    </data>
</odoo>
